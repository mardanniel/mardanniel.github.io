import SectionHeader from '@components/section-header';

export default function Contact() {
  return (
    <div id='contact' className='h-screen p-10 pt-24'>
      <SectionHeader>get in touch</SectionHeader>
    </div>
  );
}
