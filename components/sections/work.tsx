import SectionHeader from '@components/section-header';

export default function Work() {
  return (
    <div id='work' className='h-screen p-10 pt-24'>
      <SectionHeader inlineBreak>what i built so far...</SectionHeader>
    </div>
  );
}
